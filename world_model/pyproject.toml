[project]
name = "zana-world-model"
version = "0.1.0"
description = "ZANA World Model (Phase 4)"
requires-python = ">=3.12"
dependencies = [
    "neo4j>=5.24.0",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
